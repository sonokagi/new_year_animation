---
description: 承認されたプランに基づき、実際のコード修正（実装）を開始する
---

1. `EXECUTION` モードに入る。
2. `implementation_plan.md` に基づき、アトミックな編集（Atomic Edits）を意識してコードを修正する。
3. 修正後、`Strict Source Review` に従い、副作用がないか慎重に検証する。
4. 検証完了後、`walkthrough.md` を作成し、`notify_user` で報告する。
